[package]
name = "ipabuyer-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "AGPL-3.0-only"
description = "IPAbuyer cross-platform core library"

[lib]
name = "ipabuyer_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
regex = "1"
serde_json = "1"
